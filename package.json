{
  "name": "city-collect",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace backend start\" \"yarn workspace frontend dev\"",
    "dev:next": "concurrently \"yarn workspace backend start\" \"yarn workspace frontend-next dev\"",
    "build:next": "yarn workspace frontend-next build",
    "start:next": "concurrently \"yarn workspace backend start\" \"yarn workspace frontend-next start\"",
    "install:all": "yarn install && yarn workspace backend install && yarn workspace frontend install && yarn workspace frontend-next install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "cheerio": "^1.1.0",
    "mongoose": "^8.16.4"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
